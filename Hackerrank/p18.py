''' if you want to add a single element to an existing set,
we can use the .add() function.'''

if __name__ == '__main__':

    country_stamps = int(input())
    distinct_stamps = list()

    for name in range(country_stamps):
        name = distinct_stamps.append(input()) 
        
    new_distinct = set(distinct_stamps)
    print(len(new_distinct))

